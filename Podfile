# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

# ignore all warnings from all pods
inhibit_all_warnings!

target 'Synonyms' do
  use_frameworks!


  target 'SynonymsTests' do
      inherit! :search_paths
  end
end
